// components/RentalCart.js
import React from 'react';
import { Typography } from '@mui/material';

function RentalCart() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Rental Cart
      </Typography>
      {/* Display rental cart items */}
    </div>
  );
}

export default RentalCart;
